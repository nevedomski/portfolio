import { defineCloudflareConfig } from '@opennextjs/cloudflare'

/**
 * OpenNext Cloudflare Configuration
 * Optimized for portfolio site with high-quality images
 */
export default defineCloudflareConfig({
  // Configuration is minimal for now
  // Additional options can be added based on documentation
})